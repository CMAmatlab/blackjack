function [answer]=IsBlackJack(hand)
%single input, single output
%will output a 1 if it is a black jack


[value]=CardValue(hand); %check the value of the hand

if (value==21) %if the value of the hand is 21(blackjack), answer is 1
answer=1;
else
answer=0; %if not, then the answer is zero
end;
